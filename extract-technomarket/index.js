const {
    getUrls,
} = require('./extract-technomarket-url');

const {
    getTechnomarketData,
} = require('./extract-technomarket-data');

module.exports = {
    getUrls,
    getTechnomarketData,
};