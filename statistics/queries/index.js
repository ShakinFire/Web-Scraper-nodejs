const {
    orderBy,
} = require('./order.js');

const {
    filterBy,
} = require('./filter.js');

const {
    searchFor,
} = require('./search.js');

module.exports = {
    orderBy,
    filterBy,
    searchFor,
};
