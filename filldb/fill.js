const {
    vendor,
    store,
    model,
    spec,
} = require('../models');

const {
    getAllTechnopolisData,
} = require('../extract-technopolis');

const {
    getAllTechnomarketData,
} = require('../extract-technopolis');

